import { createStore } from 'vuex'
import lobby from './modules/lobby'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    lobby,
  }
})
